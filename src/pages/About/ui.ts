import styled from 'styled-components'

export const StyledWrapper = styled.section``

export const StyledSocialBlock = styled.div`
  padding: 1rem 0;
  display: flex;
  flex-direction: column;
  border-top: 1px solid var(--rc-color-border-3);
`
